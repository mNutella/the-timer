# Reference only — you don't need to copy this file.
# Running `npx convex dev` auto-generates .env.local with these values.

# Convex
CONVEX_DEPLOYMENT=dev:your-deployment-name
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud
